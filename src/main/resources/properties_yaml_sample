spring:
  datasource:
    driver-class-name: {DB명}
    url: {DBurl}
    username: {사용자계정}
    password: {비밀번호}

  flyway:
    baseline-on-migrate: true

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  jpa:
    show-sql: true

fileUrl:
  imgUrl: {이미지파일url}